import styled from 'styled-components';

const SCCard = styled.div`
  background: #FFFFFF;
  box-shadow: 0px 4px 34px rgba(82, 82, 246, 0.3);
  border-radius: 8px;
  padding: 32px;
`;

export default SCCard;